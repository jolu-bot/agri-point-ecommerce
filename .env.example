# =============================================================================
# AGRI POINT SERVICE - Configuration des Variables d'Environnement
# =============================================================================
# Copiez ce fichier en ".env.local" et remplissez avec vos vraies valeurs
# Ne committez JAMAIS le fichier .env.local contenant vos vrais secrets !
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE - MongoDB
# -----------------------------------------------------------------------------
# Connexion locale (développement)
MONGODB_URI=mongodb://localhost:27017/agripoint

# Ou MongoDB Atlas (production)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/agripoint?retryWrites=true&w=majority

# -----------------------------------------------------------------------------
# AUTHENTICATION - JWT Tokens
# -----------------------------------------------------------------------------
# Générez des secrets forts et uniques pour la production
# Utilisez: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-change-this-in-production
JWT_REFRESH_SECRET=your-refresh-secret-change-this-in-production

# -----------------------------------------------------------------------------
# AI - OpenAI (AgriBot)
# -----------------------------------------------------------------------------
# Obtenez votre clé sur: https://platform.openai.com/api-keys
# AgriBot fonctionnera en mode démo si cette clé n'est pas définie
OPENAI_API_KEY=sk-your-openai-api-key-here

# -----------------------------------------------------------------------------
# PAYMENTS - Stripe
# -----------------------------------------------------------------------------
# Obtenez vos clés sur: https://dashboard.stripe.com/apikeys
# Mode test (pk_test_ et sk_test_) pour le développement
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# -----------------------------------------------------------------------------
# PAYMENTS - PayPal
# -----------------------------------------------------------------------------
# Obtenez vos identifiants sur: https://developer.paypal.com
NEXT_PUBLIC_PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_CLIENT_SECRET=your_paypal_client_secret

# -----------------------------------------------------------------------------
# PAYMENTS - Mobile Money (Cameroun)
# -----------------------------------------------------------------------------
# MTN Mobile Money
MTN_API_KEY=your_mtn_api_key
MTN_API_SECRET=your_mtn_api_secret
MTN_SUBSCRIPTION_KEY=your_mtn_subscription_key

# Orange Money
ORANGE_API_KEY=your_orange_api_key
ORANGE_API_SECRET=your_orange_api_secret
ORANGE_MERCHANT_ID=your_orange_merchant_id

# -----------------------------------------------------------------------------
# EMAIL - SMTP Configuration
# -----------------------------------------------------------------------------
# Pour Gmail, activez "Autoriser les applications moins sécurisées"
# Ou générez un mot de passe d'application: https://myaccount.google.com/apppasswords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_password_or_app_password
EMAIL_FROM=noreply@agri-ps.com
EMAIL_FROM_NAME=AGRI POINT SERVICE

# Autres fournisseurs SMTP populaires:
# - SendGrid: smtp.sendgrid.net (port 587)
# - Mailgun: smtp.mailgun.org (port 587)
# - Mailtrap (test): smtp.mailtrap.io (port 2525)

# -----------------------------------------------------------------------------
# SITE CONFIGURATION
# -----------------------------------------------------------------------------
# URL de base du site (sans trailing slash)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Informations de contact
NEXT_PUBLIC_CONTACT_EMAIL=infos@agri-ps.com
NEXT_PUBLIC_CONTACT_PHONE=+237657393939
NEXT_PUBLIC_WHATSAPP_NUMBER=237676026601

# -----------------------------------------------------------------------------
# ANALYTICS & MONITORING (Optionnel)
# -----------------------------------------------------------------------------
# Google Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Vercel Analytics (activé automatiquement sur Vercel)
# VERCEL_ANALYTICS_ID=your_analytics_id

# Sentry (monitoring d'erreurs)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# -----------------------------------------------------------------------------
# FILE STORAGE (Optionnel)
# -----------------------------------------------------------------------------
# Cloudinary (hébergement d'images)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# AWS S3 (stockage de fichiers)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_REGION=eu-west-1
# AWS_S3_BUCKET=your-bucket-name

# -----------------------------------------------------------------------------
# DEVELOPMENT & DEBUGGING
# -----------------------------------------------------------------------------
# Mode de l'application (development | production | test)
NODE_ENV=development

# Niveau de log (error | warn | info | debug)
LOG_LEVEL=info

# Activer le mode debug pour Next.js
# DEBUG=true

# -----------------------------------------------------------------------------
# SECURITY
# -----------------------------------------------------------------------------
# Liste des origines autorisées pour CORS (séparées par des virgules)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Clé de chiffrement pour les données sensibles
ENCRYPTION_KEY=your-32-character-encryption-key

# -----------------------------------------------------------------------------
# RATE LIMITING (Optionnel)
# -----------------------------------------------------------------------------
# Nombre maximum de requêtes par IP par minute
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=60000

# -----------------------------------------------------------------------------
# CACHE (Optionnel)
# -----------------------------------------------------------------------------
# Redis (pour le cache et les sessions)
# REDIS_URL=redis://localhost:6379
# REDIS_PASSWORD=your_redis_password

# -----------------------------------------------------------------------------
# WEBHOOKS & API CALLBACKS
# -----------------------------------------------------------------------------
# URL publique pour recevoir les webhooks (production)
WEBHOOK_BASE_URL=https://your-domain.com

# -----------------------------------------------------------------------------
# FEATURES FLAGS (Optionnel)
# -----------------------------------------------------------------------------
# Activer/désactiver certaines fonctionnalités
FEATURE_AGRIBOT_ENABLED=true
FEATURE_REVIEWS_ENABLED=false
FEATURE_BLOG_ENABLED=false
FEATURE_NEWSLETTER_ENABLED=true
FEATURE_PROMOTIONS_ENABLED=true

# -----------------------------------------------------------------------------
# NOTES IMPORTANTES
# -----------------------------------------------------------------------------
# 1. Ne committez JAMAIS ce fichier avec de vraies valeurs
# 2. Ajoutez .env.local à votre .gitignore
# 3. Générez des secrets forts pour la production
# 4. Utilisez des clés API de test en développement
# 5. Stockez les secrets de production dans un gestionnaire sécurisé
#    (Vercel Secrets, AWS Secrets Manager, HashiCorp Vault, etc.)
# 6. Documentez toute nouvelle variable dans ce fichier
# 7. Vérifiez régulièrement que vos clés API sont toujours valides
# =============================================================================
